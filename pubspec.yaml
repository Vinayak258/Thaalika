name: thaalika_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter

  # UI
  cupertino_icons: ^1.0.8
  google_fonts: ^6.1.0
  
  # Firebase
  firebase_core: ^3.8.1
  firebase_auth: ^5.3.4
  cloud_firestore: ^5.6.2
  firebase_storage: ^12.2.3
  firebase_messaging: ^15.1.3
  cloud_functions: ^5.1.3
  
  # State Management
  provider: ^6.1.1
  
  # Navigation
  go_router: ^12.0.0
  
  # Utilities
  intl: ^0.19.0
  shared_preferences: ^2.2.2
  dio: ^5.3.3
  uuid: ^4.5.1
  url_launcher: ^6.3.1
  image_picker: ^1.0.7
  
  # Location Services (FREE - no paid APIs)
  geolocator: ^13.0.1
  geocoding: ^3.0.0
  google_places_flutter: ^2.0.9
  permission_handler: ^11.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  
  # Assets
  assets:
    - assets/screens/
